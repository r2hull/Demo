<!DOCTYPE html>
<html lang="en" xmlns:th="http://www.thymeleaf.org">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
        }
        .invoice-container {
            width: 800px;
            margin: 20px auto;
            padding: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 20px;
        }
        .header img {
            width: 150px;
        }
        .title {
            text-align: center;
            margin: 10px 0;
            border: .5px solid black;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #000;
            padding: 8px;
            text-align: left;
        }
        .amount {
            text-align: left;
        }
        .footer {
            margin-top: 20px;
            font-size: 14px;
            border:1px solid black;
            vertical-align: top;
        }
        .signature {
            margin-top: 40px;
            text-align: left;
            border:1px solid;
        }
    </style>
</head>
<body>
    <div class="invoice-container">
        <div class="header">
            <img th:src="@{/images/logo.png}" alt="Company Logo"/> 
            <h2>Invoice</h2>
        </div>

        <div class="title">
            <h3>Invoice Details</h3>
        </div>

        <p><strong>Invoice Number:</strong> <span th:text="${invoice.invoiceNumber}"></span></p>
        <p><strong>Date:</strong> <span th:text="${invoice.date}"></span></p>
        <p><strong>Customer Name:</strong> <span th:text="${invoice.customerName}"></span></p>
        <p><strong>Customer Address:</strong> <span th:text="${invoice.customerAddress}"></span></p>

        <table>
            <thead>
                <tr>
                    <th>Item Name</th>
                    <th>Quantity</th>
                    <th>Price</th>
                    <th>Total</th>
                </tr>
            </thead>
            <tbody>
                <tr th:each="item : ${invoice.items}">
                    <td th:text="${item.name}"></td>
                    <td th:text="${item.quantity}"></td>
                    <td th:text="${item.price}"></td>
                    <td th:text="${item.total}"></td>
                </tr>
            </tbody>
        </table>

        <p><strong>Subtotal:</strong> <span th:text="${invoice.subtotal}"></span></p>
        <p><strong>Tax:</strong> <span th:text="${invoice.tax}"></span></p>
        <p><strong>Grand Total:</strong> <span th:text="${invoice.totalAmount}"></span></p>

        <div class="footer">
            <p>Thank you for your business!</p>
        </div>

        <div class="signature">
            <p>Authorized Signature: _____________________</p>
        </div>
    </div>
</body>
</html>