@Bean
public ItemWriter<ReportManagement> reportWriter() {
    return items -> {
        // Convert Chunk<? extends ReportManagement> to List<ReportManagement>
        List<ReportManagement> reportManagementList = new ArrayList<>(items);
        reportManagementDao.saveAll(reportManagementList);
    };
}