Optional.ofNullable(orderDto.getMultiAccounts())
        .filter(multiAccounts -> !multiAccounts.isEmpty())
        .ifPresent(multiAccounts -> {
            logger.debug("Validate Multi Account Amount {}", multiAccounts);
            buildMultiAccount(orderDto.getMId(), multiAccounts, orderDto.getOrderAmount());
        });