ALTER TABLE "NIRMAL_GURJAR"."BULK_REFUND_BOOKING_DTLS" 
ADD (
    "REFUND_TYPE" VARCHAR2(50 BYTE),
    "MERCHANT_ORDER_ID" VARCHAR2(50 BYTE),
    "ATRN" VARCHAR2(50 BYTE),
    "REFUND_AMOUNT" NUMBER(20,2),
    "REFUND_CURRENCY" VARCHAR2(10 BYTE),
    "COMMENTS" VARCHAR2(500 BYTE)
);