The error during order search happened because the database has invalid values for some status fields — TransactionStatus, PaymentStatus, and RefundStatus.

The code is working fine. The issue is with the wrong data in the database,
The application behaves correctly by rejecting invalid enum values during deserialization.