when(customerDao.getCustomerByCustomerId(anyString(), anyString()))
    .thenAnswer(invocation -> {
        System.out.println("dao stub hit args=" + Arrays.toString(invocation.getArguments()));
        return testCustomerDto;
    });

@BeforeEach
void setUpContext() {
    SecurityContextHolder.clearContext();
}

@AfterEach
void cleanUpContext() {
    SecurityContextHolder.clearContext();
    Mockito.framework().clearInlineMocks(); // static mocks bhi reset ho jayenge
}