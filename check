// First check if accountIdentifier exists
        if (!MerchantMultiAccountDetails.contains(multiAccountDto.getAccountIdentifier())) {
            logger.info("validateOrderRequest, Invalid multi account :{}", multiAccountDto.getAccountIdentifier());
            addError(TransactionErrorConstants.INVALID_ERROR_CODE, 
                MessageFormat.format(TransactionErrorConstants.INVALID_ERROR_MESSAGE, 
                    "Multi Account", "Invalid account identifier: " + multiAccountDto.getAccountIdentifier()));
            throwIfErrors();  // This will throw immediately if there are errors
        }