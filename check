String fileName = file.getOriginalFilename();

if (fileName == null || !fileName.matches("^[A-Za-z0-9]+(\\.[A-Za-z0-9]+)$")) {
    throw new IllegalArgumentException("Invalid file name. Special characters and spaces are not allowed.");
}