private static Predicate getOrderStatusPredicate(Join<MerchantOrderPayment, Order> orderJoin, CriteriaBuilder criteriaBuilder, Enum<?> status) {
    return criteriaBuilder.equal(orderJoin.get("status"), status);
}