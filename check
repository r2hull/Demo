import jakarta.validation.constraints.Pattern;

public class RemarkRequest {

    @Pattern(regexp = "^[a-zA-Z0-9_@./,:\\-\\s]*$", message = "Remark contains invalid characters")
    private String remark;

    // Getter and Setter
}