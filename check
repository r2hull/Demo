JDBC exception executing SQL [SELECT * FROM (SELECT M.*,ROW_NUMBER () OVER (PARTITION BY  m.id ORDER BY m.id ) R FROM merchant_user m, merchant_entity_user  meu, merchant_entity_group meg WHERE m.id = meu.user_id AND ( ( meu.mid = ? AND meu.entity_id IS NULL ) OR ( meg.entity_id = meu.entity_id AND meg.mid = ? ))) WHERE R=1 order by R.created_at desc fetch first ? rows only] [ORA-19200: Invalid column specification

https://docs.oracle.com/error-help/db/ora-19200/] [n/a]
