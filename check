int page = 0;
int size = 10;
int startRow = page * size;
int endRow = startRow + size;

List<MerchantUserDto> dtos = merchantUserRepository.findBymIdPaged(mId, startRow, endRow)
    .stream()
    .map(this::convertEntityToDTO)
    .toList();